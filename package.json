{
  "name": "the-hr-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Recruitment and Vetting portal for a security company, with BS 7858:2019 screening built into the workflow",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run test:permissions && npm run test:tokens && npm run test:contrast && npm run db:test",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:validate": "prisma validate",
    "db:test": "bash scripts/test-constraints.sh",
    "db:seed": "tsx --env-file-if-exists=.env prisma/seed.ts",
    "db:reset": "prisma migrate reset --force --skip-generate && npm run db:seed",
    "test:permissions": "tsx scripts/test-permissions.ts",
    "test:contrast": "tsx scripts/check-contrast.ts",
    "test:tokens": "tsx scripts/test-tokens.ts",
    "test:e2e": "node tests/e2e/run.mjs",
    "setup:config": "tsx --env-file-if-exists=.env prisma/seed.ts --configuration-only",
    "setup:first-admin": "tsx --env-file-if-exists=.env scripts/setup-first-admin.ts",
    "secret:new": "tsx scripts/new-secret.ts",
    "secret:reseal": "tsx --env-file-if-exists=.env scripts/reseal.ts",
    "sweep:clocks": "tsx --env-file-if-exists=.env scripts/sweep-screening-clocks.ts",
    "sweep:duty": "tsx --env-file-if-exists=.env scripts/sweep-duty.ts",
    "demo:duty": "tsx --env-file-if-exists=.env scripts/demo-duty.ts",
    "demo:hub": "tsx --env-file-if-exists=.env scripts/demo-hub.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "jose": "^6.2.12",
    "next": "^16.3.5",
    "nodemailer": "^10.0.10",
    "qrcode-generator": "^2.0.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^22.20.3",
    "@types/nodemailer": "^8.0.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@types/web-push": "^3.6.4",
    "playwright-core": "^1.63.0",
    "postcss": "^8.5.28",
    "prisma": "^6.12.0",
    "tailwindcss": "^4.3.3",
    "tsx": "^4.23.15",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.9"
  },
  "prisma": {
    "seed": "tsx --env-file-if-exists=.env prisma/seed.ts"
  }
}
